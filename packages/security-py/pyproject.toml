[project]
name = "ventureos-security"
version = "0.0.0"
description = "Masking, redaction, and key-shape validation utilities (Python)."
requires-python = ">=3.11"
dependencies = ["ventureos-contracts"]

[tool.uv.sources]
ventureos-contracts = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ventureos_security"]
