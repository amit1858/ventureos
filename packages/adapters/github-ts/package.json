{
  "name": "@ventureos/adapter-github",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no eslint configured yet'",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@ventureos/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
