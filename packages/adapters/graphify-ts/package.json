{
  "name": "@foundry/adapter-graphify",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no eslint configured yet'",
    "test": "vitest run"
  },
  "dependencies": {
    "@foundry/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
