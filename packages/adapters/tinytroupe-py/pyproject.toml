[project]
name = "foundry-tinytroupe-adapter"
version = "0.0.0"
description = "Adapter that wraps Microsoft TinyTroupe for Foundry PersonaLab. Sprint 0: interface only."
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7",
  "foundry-contracts",
  "foundry-providers-core",
  # NOTE: tinytroupe itself is NOT a dependency yet. M1 will pin tinytroupe==0.7.0.
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.uv.sources]
foundry-contracts = { workspace = true }
foundry-providers-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/foundry_tinytroupe"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
