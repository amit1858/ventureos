{
  "name": "@ventureos/providers-anthropic",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no eslint configured yet'",
    "test": "vitest run"
  },
  "dependencies": {
    "@ventureos/contracts": "workspace:*",
    "@ventureos/providers-core": "workspace:*",
    "@anthropic-ai/sdk": "^0.27.0"
  },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0" }
}
